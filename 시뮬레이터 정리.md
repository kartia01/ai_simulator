# ClickMe 광고 시뮬레이터

| 항목 | 내용 |
|---|---|
| 발표일 | 2026-06-08 |
| 버전 | AI Engine v2.0 |
| 발표 범위 | 시뮬레이터 AI 엔진 + 프론트엔드 |

---

## 1. 서비스 한 줄 정의

> **광고를 집행하기 전, AI 가상 소비자에게 먼저 테스트해서 실패 확률을 줄이는 광고 성과 예측 플랫폼**

---

## 2. 우리가 해결하는 문제

```
기존 패러다임:  시안 제작 → 내부 검토 → 집행(예산 소진) → 결과 확인 → 실패 폐기
                                                ↑
                                        이미 비용 발생

ClickMe:        시안 제작 → 시뮬레이션(비용 0) → 방향성 확인 → 집행 → 검증
```

| 리서치 방법 | 비용 | 기간 |
|---|---|---|
| FGI (포커스그룹) | 500~2,000만원 | 3~6주 |
| 정량 설문 | 200~800만원 | 2~4주 |
| **ClickMe 시뮬레이션** | **소액** | **즉시** |

---

## 3. 시스템 아키텍처

```
┌─────────────────────────────────┐
│   Next.js 프론트엔드 (포트 3000)  │
│   SimulationDashboard / Metrics  │
└────────────────┬────────────────┘
                 │ multipart/form-data or JSON
                 ▼  POST /api/simulate
┌─────────────────────────────────┐
│   Python FastAPI AI Engine       │
│   (포트 8000)                    │
│                                  │
│  ┌──────────┐  ┌──────────────┐  │
│  │ vision.py│  │   agent.py   │  │
│  │ GPT-4o   │  │ gpt-4o-mini  │  │
│  │ Vision   │  │ (x20 병렬)   │  │
│  └──────────┘  └──────────────┘  │
│                                  │
│  ┌──────────────────────────┐    │
│  │        memory.py         │    │
│  │ text-embedding-3-small   │    │
│  │ + pgvector 유사도 검색    │    │
│  └──────────────────────────┘    │
└────────────────┬────────────────┘
                 │
┌────────────────▼────────────────┐
│   NeonDB (PostgreSQL + pgvector) │
│   personas 테이블 + persona_memories │
└─────────────────────────────────┘
```

---

## 4. 핵심 기술: 3단계 인지 루프 (Cognitive Loop)

### 아이디어

AI가 "광고 분석"을 하지 않고, **실제 사람처럼 광고에 반응**하도록 설계.
각 페르소나는 자신의 신원을 가진 채 광고를 '목격'하고, 3단계 심리 프로세스를 거친다.

```
광고 노출
    │
    ▼
┌─────────────────────────────────────────────┐
│ Step 1 — 본능 반응 (1.5초)                   │
│  - 즉각 연상 단어 3개 (한국어)                │
│  - Appeal Score 1~5 (gut-feel)              │
└────────────────────┬────────────────────────┘
                     │ appeal_score가 Step 2·3의 기준
                     ▼
┌─────────────────────────────────────────────┐
│ Step 2 — 자기중심적 필터링 (3초)              │
│  - 지금 상황·넘기는 조건 기반 이탈 판단        │
│  - sentiment   -1.0 (거부) ~ 1.0 (호감)     │
│  - comprehension 이해도 0.0~1.0             │
│  - recall      브랜드 기억 가능성 0.0~1.0    │
└────────────────────┬────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│ Step 2.5 — 내부 트리거 트리아드 (0.5초)       │
│  ① 욕구/필요성: 지금 이게 필요한가?           │
│  ② 예산 확인: 이 가격이 내 지갑에 맞는가?     │
│  ③ 브랜드 신뢰: 이 브랜드를 믿을 수 있는가?  │
│  → reasoning_chain 한 문장 내부 독백         │
└────────────────────┬────────────────────────┘
                     │
                     ▼
┌─────────────────────────────────────────────┐
│ Step 3 — 최종 행동                           │
│  - clicked: 실제로 탭했는가 (bool)           │
│  - conversion_intent: 구매·가입 의향 (bool)  │
│  - impression: 솔직한 한 줄 느낌             │
│  - confidence 0.0~1.0                       │
└─────────────────────────────────────────────┘
```

---

## 5. 기술 상세 — 차별화 포인트 5가지

### 5-1. 동적 Temperature 할당 (페르소나별 개성 차이)

단순히 같은 temperature로 모든 페르소나를 실행하면 응답이 동질화된다.
각 페르소나의 심리 특성에 따라 variability를 다르게 부여:

| 조건 | 효과 |
|---|---|
| 충동구매 성향 | temperature +0.15 (즉흥적·가변적) |
| deal_prone_score 높음 | temperature 최대 +0.10 (가격 자극에 감정적) |
| 브랜드 충성도 낮음 | temperature +0.10 (더 흔들리는 반응) |
| 50대 이상 | temperature -0.10 (보수적·안정적) |
| 30세 미만 | temperature +0.10 (충동적) |

범위: 0.50 ~ 1.10으로 클램프

### 5-2. 논리 모순 자동 보정 (신뢰도 보장)

LLM이 논리적으로 모순된 응답을 생성할 때 자동 교정:

```
appeal_score = 1  →  is_dropped_out 강제 True
is_dropped_out = True  →  clicked 강제 False
clicked = False  →  conversion_intent 강제 False
appeal_score ≤ 2  →  clicked 강제 False
awareness 캠페인  →  conversion_intent 항상 False
```

### 5-3. IQR 이상치 필터링 (통계적 품질 관리)

20명 이상의 시뮬레이션에서 attention 점수 기준으로
IQR(사분위 범위) 1.5배 바깥의 이상치를 자동 제거.
이상치 제거 후 메트릭 계산 → 극단값 왜곡 방지.

### 5-4. pgvector 기반 페르소나 메모리 (Paper 1 — Generative Agents)

각 페르소나가 광고를 볼 때마다 기억이 누적된다.
*Park et al. (2023) Generative Agents* 의 메모리 아키텍처를 광고 시뮬레이션에 적용.

**메모리 저장 — Importance 점수 포함**

```python
# 반응 결과에 따라 중요도 자동 계산
tag:        "PURCHASE" | "EVENT" | "REFLECTION"
importance: 9 (구매 의향) | 7 (클릭) | 6 (호감 미클릭) | 4 (강한 거부) | 3 (스크롤)
content:    "광고 노출: ... | 즉각 감정: ... | 행동: 클릭 후 구매 의향 있음"
embedding:  text-embedding-3-small (1536차원)
```

**3-Factor 검색 공식 (기존 2-factor → 개선)**

```
이전: score = cosine_similarity × time_decay
이후: score = (recency + importance_norm + relevance) / 3

  recency        = time_decay(half_life=30일)   — 최신일수록 높음
  importance_norm = (importance - 1) / 9        — 1~10 → 0~1 정규화
  relevance      = cosine_similarity            — 현재 광고와 관련성
```

**Reflection 자동 생성**

페르소나의 누적 이벤트가 5의 배수에 도달할 때, 최근 5개 기억을 합성해 상위 레벨 인사이트를 저장:

```
[REFLECTION, importance=8]
"이 페르소나는 뷰티 카테고리 광고에 반복 노출되었으나 가격 임계값 초과로 클릭을 자제하는 패턴을 보인다."
```

→ 다음 시뮬레이션 때 Reflection이 높은 importance 점수로 조회되어 반응에 자연스럽게 반영된다.

### 5-5. Confidence 보정 (Paper 3 — Agentic Confidence Calibration)

LLM이 생성한 raw confidence는 모든 페르소나에 균일하게 분포하는 경향이 있다.
페르소나 특성에 따라 사후 보정을 적용해 confidence를 실제 행동 패턴과 정합시킨다:

| 조건 | 보정 | 이유 |
|---|---|---|
| `brand_loyalty < 0.3` | ×0.85 | 브랜드 신뢰 없음 → 확신 감소 |
| `age > 55` | ×0.90 | 보수적 연령대 → 신중한 판단 |
| `appeal_score == 5` | ×1.10 | 강한 본능 반응 → 확신 증폭 |
| `deal_prone ≥ 0.7 + appeal ≥ 4` | ×1.05 | 가격 민감 + 높은 관심 → 확신 소폭 증폭 |

---

## 6. MBTI 기반 자기중심 필터

페르소나의 MBTI에 따라 광고를 판단하는 기준이 달라진다:

| MBTI | 판단 기준 |
|---|---|
| ISFJ | "이 브랜드가 진심으로 느껴지는가? 우리 가족 생활에 어울리는가?" |
| INTP | "데이터가 납득되는가?" |
| ESTP | "지금 당장 뭔가 이득이 있는가?" |
| ENFP | "내 가치관과 공명하는가?" |
| INTJ | "이게 진짜 효과가 있는가?" |

---

## 7. 출력 메트릭 — 프론트엔드 표시 지표

| 지표 | 정의 | 기준 |
|---|---|---|
| **VTR** (조회 완료율) | 이탈하지 않은 비율 (sentiment≥0 또는 clicked) | ≥25% 평균, ≥35% 우수 |
| **CTR** (클릭률) | click_intent=True 비율 | ≥1.5% 평균, ≥3% 우수 |
| **CVR** (전환율) | conversion_intent=True 비율 | - |
| **이탈률** | click_intent=False & sentiment<0 | <70% 허용 |
| **매력도** | appeal_score 평균 (1~5) | ≥3.0 평균, ≥4.0 우수 |

---

## 8. 구현 현황

### 완료

| 구성 요소 | 상태 |
|---|---|
| 3단계 인지 루프 (agent.py) | ✅ 완료 |
| 동적 temperature 할당 | ✅ 완료 |
| 논리 모순 자동 보정 | ✅ 완료 |
| IQR 이상치 필터 | ✅ 완료 |
| 이미지/영상 Vision 분석 (GPT-4o) | ✅ 완료 |
| pgvector 메모리 시스템 | ✅ 완료 |
| Memory Importance 점수 + 3-Factor 검색 (Paper 1) | ✅ 완료 |
| Reflection 자동 생성 — 5회 누적 시 패턴 합성 (Paper 1) | ✅ 완료 |
| Confidence 페르소나 특성 기반 사후 보정 (Paper 3) | ✅ 완료 |
| MBTI 기반 필터 | ✅ 완료 |
| 플랫폼별 행동 묘사 (인스타·유튜브·틱톡·네이버·카카오) | ✅ 완료 |
| 비한국어 감지 & 재시도 | ✅ 완료 |
| Rate Limit 지수 백오프 | ✅ 완료 |
| 동시성 제어 (Semaphore 15) | ✅ 완료 |
| FastAPI /api/simulate 엔드포인트 | ✅ 완료 |
| multipart/form-data + JSON 양방향 처리 | ✅ 완료 |
| Next.js 시뮬레이션 대시보드 UI | ✅ 완료 |
| MetricsPanel (VTR/CTR/이탈/매력도) | ✅ 완료 |
| 인지 퍼널 타임라인 (CognitiveTimeline) | ✅ 완료 |
| 페르소나별 피드백 카드 (PersonaFeedbackCard) | ✅ 완료 |
| 키워드 빈도 분석 (전체 emotions 집계) | ✅ 완료 |
| 페르소나 시드 데이터 (DB) | ✅ 완료 |

### 미완료 / 다음 단계 (Phase 2)

| 기능 | 목표 |
|---|---|
| Debate Agent (긍정 편향 제거) | 2026-07-08 |
| 랜딩페이지 URL 분석 (Playwright) | 2026-07-08 |
| Java Spring Boot 백엔드 + JWT 인증 | 2026-07-08 |
| A/B 광고 비교 기능 | 미정 |
| PDF 리포트 실제 생성 | 2026-07-08 |
| 광고 생성 AI | 후순위 |

---

## 9. 데모 플로우

### 9-1. 기본 텍스트 시뮬레이션

1. 프론트엔드 접속 (`http://localhost:3000`)
2. **광고 내용** 텍스트 입력:
   ```
   헤드라인: 30일 만에 -10kg, 다이어트 보조제 '슬림핏'
   CTA: 지금 첫 구매 50% 할인
   비주얼: 전후 사진, 밝은 녹색 배경
   ```
3. **시뮬레이션 실행** 클릭
4. 결과 확인: 메트릭 패널 → 인지 퍼널 → 페르소나별 반응 카드

### 9-2. 이미지 시뮬레이션

1. 광고 이미지(JPG/PNG, 최대 10MB) 드래그 앤 드롭
2. 추가 텍스트 설명 선택 입력
3. 시뮬레이션 실행 → GPT-4o Vision이 이미지 분석 후 페르소나에게 전달

### 9-3. 주목할 포인트

- 페르소나 카드의 **속마음 인용구**: AI가 분석이 아니라 사람처럼 반응
- **연상 단어 3개**: 즉각적 감정 반응
- **reasoning_chain**: 욕구→예산→브랜드 판단 내부 독백
- 동일 광고도 MBTI·연령·상황에 따라 다른 반응

---

## 10. API 스펙 요약

### POST /api/simulate

**요청** (multipart/form-data 또는 JSON):

| 필드 | 타입 | 설명 |
|---|---|---|
| adId | string | 광고 식별자 |
| adContent | string | 광고 텍스트 (최소 10자, 미디어 있으면 선택) |
| adType | "IMAGE" \| "VIDEO" | 광고 유형 |
| objective | "conversion" \| "awareness" | 캠페인 목적 |
| productPrice | int (선택) | 상품 가격 (원) |
| mediaFile | File (선택) | 이미지/영상 파일 |
| personaIds | string[] (선택) | 특정 페르소나 지정 (미입력 시 전체) |

**응답**:

```json
{
  "adId": "ad-1749358800000",
  "totalPersonas": 20,
  "metrics": {
    "vtr": 65.0,
    "ctr": 25.0,
    "cvr": 10.0,
    "dropoutRate": 35.0,
    "avgAppealScore": 3.2,
    "avgAttention": 0.55,
    "avgSentiment": 0.12,
    "outlierCount": 0
  },
  "results": [
    {
      "personaId": "p_001",
      "personaName": "김지현",
      "personaAge": 28,
      "personaJob": "직장인",
      "segment": "20s_female",
      "attention": 0.75,
      "sentiment": 0.6,
      "clickIntent": true,
      "conversionIntent": false,
      "emotions": ["솔깃", "반신반의", "궁금"],
      "reasoning": "할인이 크긴 한데 전후 사진이 너무 과장된 것 같아서...",
      "impression": "관심은 가지만 믿기 어렵다",
      "mbti": "ENFP"
    }
  ]
}
```

---

## 11. 기술 선택 이유

| 선택 | 이유 |
|---|---|
| **gpt-4o-mini** (시뮬레이션) | 비용 최적화: 20명 병렬 호출 → 저렴하고 빠름 |
| **GPT-4o Vision** (이미지 분석) | 이미지 이해 품질이 중요한 한 번의 분석 |
| **asyncio.Semaphore(15)** | OpenAI Rate Limit 안에서 최대 병렬 처리 |
| **pgvector** | 페르소나 메모리 벡터 유사도 검색 |
| **Pydantic 검증** | JSON 파싱 실패 → 자동 재시도 로직과 연동 |
| **JSON response_format** | 구조화된 출력 강제 → 파싱 실패율 최소화 |

---

## 12. 리스크 & 대응 현황

| 리스크 | 대응 |
|---|---|
| LLM 논리 모순 | `_fix_logic_errors()` 자동 보정 |
| 비한국어 응답 | 정규식 감지 후 최대 3회 재시도 |
| JSON 파싱 실패 | `KeyError/ValidationError` catch → 재시도 |
| Rate Limit | 지수 백오프 (5초·10초·20초) |
| 페르소나 응답 동질화 | 동적 temperature + MBTI 필터 |
| 이상치 왜곡 | IQR 기반 자동 제거 |

---

## 13. 결론

### 우리가 만든 것

광고를 집행하기 전, **AI 가상 소비자 20명이 실제 사람처럼 반응**하는 시뮬레이터를 구현했다.

단순한 "AI 분석"이 아니라, 각 페르소나가 자신의 연령·직업·MBTI·소비 성향·과거 기억을 바탕으로 광고를 **목격하고, 판단하고, 행동**하는 3단계 인지 루프를 통해 현실에 가까운 반응을 생성한다.

### Phase 1 에서 검증한 것

| 검증 항목 | 결과 |
|---|---|
| 페르소나별 다양한 반응 | 동적 temperature + MBTI 필터로 동질화 방지 |
| 논리적으로 일관된 출력 | 자동 보정 로직으로 모순 응답 0건 목표 |
| 대규모 병렬 처리 | Semaphore 15 + 지수 백오프로 Rate Limit 내 안정 처리 |
| 이미지 광고 지원 | GPT-4o Vision 연동으로 텍스트·이미지 양방향 지원 |
| 누적 경험 반영 | pgvector 메모리 + Importance 기반 3-Factor 검색으로 반복 노출 효과 시뮬레이션 |
| 신뢰도 높은 기억 구조 | Reflection 자동 합성으로 페르소나 장기 행동 패턴 누적 (Paper 1 적용) |
| Confidence 현실화 | 페르소나 특성 기반 보정으로 raw LLM 수치보다 정확한 확신도 출력 (Paper 3 적용) |

### 이것이 의미하는 것

> **광고주는 수백만 원의 리서치 비용 없이, 즉시 시안의 방향성을 검증할 수 있다.**

FGI·설문 대비 비용은 소액, 기간은 즉시. 실패 가능성이 높은 시안을 집행 전에 걸러내는 것이 ClickMe의 핵심 가치다.

### 다음 단계 (Phase 2)

Phase 1이 "반응 생성"이었다면, Phase 2는 **신뢰도 강화**와 **서비스화**다.

- **Debate Agent**: 긍정 편향을 능동적으로 반박하는 대립 에이전트 도입
- **랜딩페이지 분석**: 광고 클릭 후 이어지는 경험까지 시뮬레이션
- **A/B 비교**: 두 시안을 동시에 돌려 수치로 비교
- **Java Spring Boot + JWT**: 실서비스 수준의 백엔드 및 인증 체계

Phase 1 베이스라인 목표일: **2026-06-12**
