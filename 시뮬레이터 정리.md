# Ad Simulator (ClickMe) 코드 전체 정리

## 1. 프로젝트 개요

광고 소재를 AI 페르소나에게 시뮬레이션하여, 실제 캠페인 집행 전에 반응·KPI를 예측하는 서비스.

- **서비스명:** ClickMe
- **아키텍처:** Next.js 프론트엔드 + FastAPI AI 엔진
- **AI:** OpenAI GPT-4o-mini (텍스트, 비전), text-embedding-3-small
- **DB:** PostgreSQL + pgvector

---

## 2. 디렉토리 구조

```
ad_simulator/
├── ai-engine/                  # 백엔드 (FastAPI)
│   ├── main.py                 # API 서버 진입점
│   ├── requirements.txt
│   └── app/
│       ├── agent.py            # 시뮬레이션 핵심 로직 (캐스케이드 파이프라인)
│       ├── db.py               # PostgreSQL 연결 및 페르소나 쿼리
│       ├── memory.py           # 생성적 에이전트 메모리 시스템
│       ├── prompts.py          # AI 프롬프트 빌더
│       ├── schemas.py          # Pydantic 데이터 모델
│       └── vision.py           # 이미지/영상 분석 (OpenAI Vision)
└── frontend/                   # 프론트엔드 (Next.js 14)
    └── src/
        ├── app/
        │   ├── layout.jsx
        │   └── page.jsx        # 뷰 라우터
        ├── api/
        │   └── simulationApi.js
        ├── hooks/
        │   └── useSimulation.js
        └── components/
            ├── WelcomeScreen.jsx
            ├── SimulationDashboard.jsx
            ├── PersonaCreator.jsx
            ├── AdImageCreator.jsx
            ├── MetricsPanel.jsx
            ├── CognitiveTimeline.jsx
            └── PersonaFeedbackCard.jsx
```

---

## 3. 백엔드 (ai-engine)

### 3.1 `main.py` — FastAPI 앱

**역할:** HTTP API 서버

**주요 함수**

| 함수 | 역할 |
|------|------|
| `_apply_target_filter()` | 나이·성별·플랫폼 기준으로 페르소나 필터링 |
| `_run()` | 시뮬레이션 실행 조율 |
| `_camel_dict()` | 응답을 snake_case → camelCase 변환 |

**API 엔드포인트**

| 메서드 | 경로 | 설명 |
|--------|------|------|
| `POST` | `/api/simulate` | 광고 시뮬레이션 실행 (JSON 또는 multipart) |
| `GET` | `/api/personas` | 저장된 페르소나 목록 조회 |
| `GET` | `/api/health` | 서비스 헬스체크 |
| `GET` | `/api/health/openai` | OpenAI API 연결 확인 |

**POST `/api/simulate` 입력**
- 광고 내용(최소 10자) 또는 미디어 파일 필수
- 파일 크기 제한: 이미지 10MB, 영상 50MB
- 미디어 없으면 JSON, 있으면 multipart/form-data

**환경변수**
```
OPENAI_API_KEY=sk-...
DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD
FRONTEND_ORIGIN=http://localhost:3000
```

---

### 3.2 `schemas.py` — 데이터 모델

#### 3단계 캐스케이드 반응 구조

**Step 1 — 무의식 반응 (1.5초)**
```python
UnconsciousReaction:
  emotions: list[str]       # 한국어 감정 단어 3개 (분석 없이 직감)
  appeal_score: int         # 1~5 (직감 점수)
```

**Step 2 — 자기중심 필터링 (3초)**
```python
SelfishFiltering:
  is_dropped_out: bool      # True = 그냥 스크롤
  reason: str               # 속마음 독백 (한국어 구어체)
  sentiment: float          # -1.0 ~ 1.0
  comprehension: float      # 0.0 ~ 1.0
  recall: float             # 0.0 ~ 1.0
```

**Step 3 — 최종 행동**
```python
FinalAction:
  clicked: bool
  conversion_intent: bool
  action_reason: str        # 엄지를 움직인 단 하나의 생각
  impression: str           # 광고에 대한 솔직한 한 줄 평
  confidence: float         # 0.0 ~ 1.0
  reasoning_chain: str      # 욕구 → 예산 → 브랜드 결정 독백
```

**`PersonaReactionSignal`** — 분석 시스템 출력 스펙 (IO Spec v1.4 §3-A)
```
schema_version: "1.4"
producer_id: "sim_cognitive_loop_v2"

P0 필수: attention, sentiment, click_intent, conversion_intent
P1 권장: comprehension, reasoning, recall
P2 선택: emotions, confidence, impression, reasoning_chain
페르소나 정보: persona_id, name, age, job, segment
```

**`AdMetrics`** — 집계 KPI
```
vtr            # 조회 완료율 (%)
ctr            # 클릭률 (%)
cvr            # 전환율 (%)
dropout_rate   # 2단계 이탈률 (%)
avg_appeal_score
avg_attention
avg_sentiment
outlier_count
benchmark_ctr, benchmark_status  # 플랫폼별 기준 비교
```

**`Conclusion`** — AI 생성 집행 권고
```
verdict:    "집행 권장" | "수정 후 재검토" | "집행 비권장"
reason:     2~3문장 판단 근거
strengths:  강점 2~3개
weaknesses: 약점 2~3개
```

**`PersonaInput`** — 페르소나 정의
```
필수: persona_id, name, age(13-80), job, context, drop_off_trigger
인구통계: gender, interests
구매행동: purchase_pattern, deal_prone_score(0-1), price_threshold(원), brand_loyalty(0-1)
상황: platform, emotional_state
```

---

### 3.3 `agent.py` — 시뮬레이션 엔진

#### 핵심 함수 목록

| 함수 | 역할 |
|------|------|
| `run_cascade_simulation()` | 메인 오케스트레이터 |
| `_simulate_one()` | 페르소나 1명 시뮬레이션 |
| `_assign_temperature()` | 페르소나별 GPT temperature 동적 할당 |
| `_fix_logic_errors()` | 심리 일관성 강제 교정 |
| `_has_foreign_text()` | 비한국어 텍스트 감지 (재시도 트리거) |
| `_compute_segment()` | 인구통계 세그먼트 생성 (`30s_female` 등) |
| `_calibrate_confidence()` | 신뢰도 사후 보정 |
| `_compute_importance()` | 메모리 중요도 점수 산출 |
| `_filter_outliers()` | IQR 기반 이상치 제거 |
| `_calc_metrics()` | VTR/CTR/CVR 등 KPI 계산 |
| `_generate_conclusion()` | AI 집행 권고 생성 |
| `_save_simulation_memories()` | 반응 결과 메모리 저장 (비동기) |

#### Temperature 할당 로직
```
기본값: 0.70
충동구매 패턴: +0.15
deal_prone_score: (score - 0.5) × 0.20
brand_loyalty: (1.0 - score) × 0.10
나이 < 30: +0.10  /  나이 > 50: -0.10
범위: 0.50 ~ 1.10
```

#### 심리 일관성 교정 규칙
```
appeal_score = 1 → is_dropped_out = true
is_dropped_out = true → clicked = false, conversion_intent = false
clicked = true → appeal_score ≥ 3
objective = awareness → conversion_intent = false (강제)
```

#### 메트릭 계산
```
VTR = (sentiment ≥ 0 또는 click_intent = true인 페르소나 수 / 전체) × 100
CTR = (clicked = true인 페르소나 수 / 전체) × 100
CVR = (conversion_intent = true인 페르소나 수 / 전체) × 100
Dropout = (not-clicked AND sentiment < 0) / 전체 × 100
평균 매력도 = attention × 4 + 1  (역변환)
```

#### 플랫폼별 CTR 기준
```
Instagram/Facebook: 1.0%  |  YouTube: 0.3%  |  TikTok: 0.8%
Naver: 1.5%  |  KakaoStory: 0.8%  |  기본: 0.8%
```

#### 집행 권고 기준
```
VTR ≥ 35% → 집행 권장 검토
VTR < 25% AND CTR < 1.5% → 집행 비권장 검토
```

#### 동시 실행 구조
- 페르소나별 병렬 메모리 조회 (asyncio.gather)
- 페르소나별 병렬 시뮬레이션 (세마포어 15개 제한)
- 결론 생성: 비동기 (메인 응답 대기)
- 메모리 저장: fire-and-forget (응답 이후 백그라운드)

#### 재시도 정책
```
JSON 파싱/검증 실패: 3회 (1초 대기)
Rate limit: 지수 백오프 (2^n × 5초)
비한국어 감지: 3회 재시도
인증 오류: 즉시 실패
```

---

### 3.4 `db.py` — 데이터베이스 레이어

**연결:** asyncpg + PostgreSQL (pgbouncer 호환, statement_cache_size=0)

**주요 함수**

| 함수 | 역할 |
|------|------|
| `init_db()` | 커넥션 풀 초기화 |
| `close_db()` | 풀 종료 |
| `init_memory_table()` | pgvector 확장 및 persona_memories 테이블 생성 |
| `get_all_personas()` | 전체 페르소나 조회 |
| `get_personas_by_ids(ids)` | ID 배열로 페르소나 조회 (SQL 인젝션 방지) |

**데이터베이스 스키마**
```sql
personas (id, name, age, job, context, drop_off_trigger,
          gender, purchase_pattern, deal_prone_score, price_threshold,
          brand_loyalty, platform, emotional_state)

persona_interests (persona_id, interest)

persona_memories (
  id UUID,
  persona_id TEXT,
  tag TEXT,              -- EVENT | PURCHASE | REFLECTION | CONVERSATION
  content TEXT,
  embedding vector(1536),
  importance SMALLINT,   -- 1~10
  created_at TIMESTAMPTZ
)
-- 인덱스: persona_id, embedding (ivfflat cosine)
```

---

### 3.5 `memory.py` — 생성적 에이전트 메모리 (논문1 기반)

**임베딩 모델:** text-embedding-3-small (1536차원)
**조회:** 후보 10개 → 점수 계산 후 상위 5개 반환
**메모리 감쇠 반감기:** 30일

#### 기억 중요도 점수
```
전환 의도 있음: 9점
클릭함: 7점
긍정 반응 (클릭 없음): 6점
강한 거부감: 4점
그냥 스크롤: 3점
```

#### 메모리 점수 공식 (3요소 평균)
```
recency      = exp(-ln(2) × 경과일 / 30)    # 최신성 (0~1)
importance   = (중요도 - 1) / 9.0            # 중요도 정규화 (0~1)
relevance    = cosine_similarity(임베딩)      # 관련성 (0~1)

최종 score = (recency + importance + relevance) / 3
```

#### 메모리 태그
```
EVENT       — 광고 노출 반응 이벤트
PURCHASE    — 전환 의도 발생
REFLECTION  — 5회 이벤트마다 패턴 요약 반성
CONVERSATION — 기타 대화
```

---

### 3.6 `prompts.py` — 프롬프트 빌더

#### 플랫폼별 행동 컨텍스트
```
Instagram  → 피드 스크롤 중, 광고는 일반 게시물처럼 위장
YouTube    → 영상 앞 강제 광고, 5초 후 스킵 버튼
TikTok     → 풀스크린 자동재생, 위로 스와이프하면 다음
Naver      → 검색 결과 사이 텍스트/이미지 배너
```

#### 시스템 프롬프트 핵심 규칙
```
❌ 금지: 분석적·마케팅 언어
❌ 금지: 비한국어 텍스트
❌ 금지: 문어체·보고서 형식
✅ 필수: 이 페르소나 자신으로서 반응
✅ 필수: 감정 상태가 모든 반응에 반영
✅ 필수: 모든 문자열 한국어 구어체
```

#### 유저 프롬프트 구성 (3단계 통합)
```
Step 1: 1.5초 안에 연상 단어 3개 + appeal_score
Step 2: 현재 상황 기반 속마음 (sentiment, comprehension, recall)
Step 2.5: 내부 트리거 체인 (욕구 → 예산 → 브랜드)
Step 3: 클릭? 구매/가입 의향? (awareness 캠페인은 conversion_intent=false 강제)
```

**조건부 요소:**
- 가격 라인: conversion 목표일 때만 포함
- DPP 주석: deal_prone_score ≥ 0.7이면 할인 민감성 강조
- 프로필 블록: 관심사, 구매 패턴, 예산, 브랜드 충성도
- 메모리 블록: 관련 과거 기억 (bullet list)

---

### 3.7 `vision.py` — 비전 분석

**역할:** 이미지/영상을 분석하여 `ad_content`를 보강

| 함수 | 역할 |
|------|------|
| `build_visual_ad_description()` | 라우터 (이미지/영상 분기) |
| `_analyze_image()` | gpt-4o-mini vision → 레이아웃·색상·CTA·분위기 분석 |
| `_analyze_video()` | 3개 키프레임(5%, 50%, 95%) 추출 후 각 분석 → 통합 |
| `_extract_key_frames()` | OpenCV로 base64 영상 디코딩 → 프레임 추출 |

**이미지 분석 항목:** 헤드라인/텍스트, 색상·레이아웃, CTA, 전체 메시지·분위기, 타겟층 첫인상

---

## 4. 프론트엔드 (frontend)

### 4.1 `page.jsx` — 뷰 라우터

**뷰 상태:** `welcome` | `simulation` | `personas` | `ad-image`

**페르소나 상태 관리 (localStorage 영속)**
```
myPersonas      — 사용자 생성 페르소나 배열
activePersonaIds — 현재 활성화된 페르소나 ID Set
```

**핸들러**
```
handleStart(content)      → initialContent 설정 후 simulation 뷰
handleNavigate(dest)      → 뷰 전환
handleAddPersona()        → 추가 후 localStorage 저장
handleDeletePersona()     → 삭제 후 localStorage 저장
handleUpdatePersona()     → 수정 후 localStorage 저장
handleTogglePersona(id)   → activePersonaIds 토글
```

---

### 4.2 `simulationApi.js` — API 클라이언트

**BASE_URL:** `process.env.NEXT_PUBLIC_API_URL ?? "http://localhost:8000/api"`

**`runSimulation(payload)`**
- `mediaFile` 있음 → FormData (multipart)
- `mediaFile` 없음 → JSON
- 에러 시 응답 JSON의 message 필드 추출

**`fetchPersonas()`**
- GET `/personas` 호출

---

### 4.3 `useSimulation.js` — 상태 훅

```
state: { result, loading, error }
simulate(payload) — runSimulation() 호출 후 상태 업데이트
reset()           — result, error 초기화
```

---

### 4.4 `WelcomeScreen.jsx` — 랜딩 페이지

**기능**
- 텍스트에어리어 입력 (광고 내용)
- Enter키 → 제출
- 빠른 제안 칩: "광고 시뮬레이션을 해줘", "페르소나를 추가하고 싶어", "광고 이미지를 만들어줘"
- 입력 파싱: 30자 미만이거나 네비게이션 키워드 포함 시 빈 문자열로 처리

---

### 4.5 `SimulationDashboard.jsx` — 메인 시뮬레이션 UI

**레이아웃**
- 좌측 사이드바 (w-60): 테스트 히스토리
- 우측 메인: 폼 + 결과

**폼 구성**
1. 미디어 업로드 (드래그앤드롭 or 파일 선택)
2. 광고 내용 텍스트에어리어 (미디어 없으면 10자 이상 필수)
3. 광고 타입 토글 (IMAGE / VIDEO, 파일 타입으로 자동 선택)
4. 페르소나 선택 체크박스 그리드

**결과 패널 (결과 존재 시)**
1. MetricsPanel — VTR, CTR, 이탈, 매력도 KPI 카드
2. CognitiveTimeline — 3단계 퍼널 시각화
3. KeywordFrequency — 감정 단어 상위 12개
4. PersonaFeedbackCard 그리드 — 개별 페르소나 반응
5. ConclusionPanel — 집행 권고 + 강점/약점

**히스토리 사이드바**
- 클릭 시 해당 결과로 전환
- 광고 내용 요약, 판정 뱃지, 타임스탬프 표시

---

### 4.6 `MetricsPanel.jsx` — KPI 카드

| 지표 | 평균 기준 | 우수 기준 |
|------|---------|---------|
| VTR (조회 완료율) | ≥ 25% | ≥ 35% |
| CTR (클릭률) | ≥ 1.5% | ≥ 3% |
| 이탈 (2단계 이탈률) | < 70% | — |
| 매력도 (직감 점수) | ≥ 3.0 | ≥ 4.0 |

---

### 4.7 `CognitiveTimeline.jsx` — 3단계 퍼널

```
Stage 1 (100%):    무의식 반응 — 전체 노출
Stage 2 (VTR%):    자기중심 필터링 — 잔류자
Stage 3 (CTR%):    최종 행동 — 클릭자
```
- 단계별 수평 바 차트
- 단계 사이 이탈 수 레이블

---

### 4.8 `PersonaFeedbackCard.jsx` — 개별 페르소나 반응 카드

```
헤더: 이름, 나이, 직업, 행동 뱃지 (✓ 클릭 / ✗ 이탈 / – 무시)
Step 1: 감정 단어 + 매력도 점 (5개)
Step 2: 속마음 (블록쿼트, sentiment로 색상)
Step 3: 인상
```

**매력도 변환:** `attention(0~1) × 4 + 1` → 1~5점 표시

---

### 4.9 `PersonaCreator.jsx` — 페르소나 관리

**폼 필드**

| 섹션 | 필드 |
|------|------|
| 기본 정보 | 이름(필수), 나이(13-80, 필수), 직업(필수), 성별 |
| 플랫폼/감정 | 플랫폼(드롭다운), 감정 상태 |
| 상황/트리거 | 상황(필수), 이탈 트리거(필수) |
| 관심사 | 쉼표 구분 텍스트 → 배열 변환 |
| 구매 행동 | 가격 민감도(0~1), 팬덤(0~1), 최대 예산(원), 구매 패턴 |

**저장:** `crypto.randomUUID()`로 신규 ID 생성

---

## 5. 데이터 흐름

### 5.1 전체 시뮬레이션 흐름

```
[WelcomeScreen] 입력
    ↓
[SimulationDashboard] 폼 제출
    ↓
[simulationApi.runSimulation()] → POST /api/simulate
    ↓
[main.py] 파싱 및 유효성 검사
    ↓
[agent.py: run_cascade_simulation()]
    ├─ 미디어 있음 → [vision.py] 이미지/영상 분석 → ad_content 보강
    ├─ 병렬 메모리 조회 → [memory.py: retrieve_memories()]
    ├─ 병렬 페르소나 시뮬레이션 (세마포어 15)
    │     ↓ [agent.py: _simulate_one()]
    │     ├─ temperature 할당
    │     ├─ system/user 프롬프트 빌드 [prompts.py]
    │     ├─ gpt-4o-mini 호출 (JSON mode)
    │     ├─ 응답 파싱 → 심리 일관성 교정 → 비한국어 체크
    │     └─ PersonaReactionSignal 반환
    ├─ IQR 이상치 제거
    ├─ AdMetrics 계산
    ├─ Conclusion 생성 (비동기 gpt-4o-mini)
    └─ 메모리 저장 (백그라운드)
    ↓
[useSimulation hook] 상태 업데이트
    ↓
[SimulationDashboard] 결과 렌더링
```

### 5.2 페르소나 라이프사이클

```
[PersonaCreator] 작성 → localStorage 저장
    ↓
[page.jsx] myPersonas, activePersonaIds 관리
    ↓
[SimulationDashboard] 체크박스로 활성/비활성
    ↓
시뮬레이션 시 activePersonaIds 필터링 → customPersonas로 전송
```

### 5.3 메모리 누적 흐름

```
시뮬레이션 1: 운동화 광고 → 전환 의도 → 중요도 9 → persona_memories 저장
    ↓ 10일 경과
시뮬레이션 2: 스포츠 브랜드 광고
    → retrieve_memories(유사 쿼리)
    → 과거 기억 점수 = (time_decay(10일) + importance_norm(0.9) + relevance(0.8)) / 3 ≈ 0.83
    → 시스템 프롬프트에 주입 → 과거 경험 반영한 반응
    ↓ 30일 경과 (반감기)
시뮬레이션 3:
    → time_decay(30일) = 0.5 → 점수 하락 → 영향 감소
```

---

## 6. API 계약 (프론트↔백엔드)

### 요청 (JSON 모드)
```json
{
  "adId": "ad-1718000000000",
  "adContent": "새로운 운동화를 소개합니다...",
  "adType": "IMAGE",
  "objective": "conversion",
  "productPrice": 89000,
  "personaIds": [],
  "customPersonas": [
    {
      "persona_id": "uuid",
      "name": "김지은",
      "age": 28,
      "job": "프리랜서",
      "platform": "Instagram",
      "context": "점심시간에 인스타그램 스크롤 중",
      "drop_off_trigger": "관심 없거나 비싼 상품",
      "deal_prone_score": 0.7,
      "brand_loyalty": 0.5,
      "price_threshold": 100000
    }
  ]
}
```

### 응답 (camelCase 변환)
```json
{
  "adId": "ad-1718000000000",
  "totalPersonas": 3,
  "metrics": {
    "vtr": 66.7, "ctr": 33.3, "cvr": 25.0,
    "dropoutRate": 33.3, "avgAppealScore": 3.67,
    "benchmarkStatus": "상회"
  },
  "results": [
    {
      "personaName": "김지은",
      "attention": 0.667,
      "sentiment": 0.5,
      "clickIntent": true,
      "conversionIntent": true,
      "emotions": ["세련된", "신기한", "예쁜"],
      "impression": "운동화치고 스타일리시하고 좋다"
    }
  ],
  "conclusion": {
    "verdict": "집행 권장",
    "reason": "VTR이 높고 CTR도 훌륭합니다.",
    "strengths": ["세련된 디자인", "합리적 가격대"],
    "weaknesses": ["일부 가격 비교 우려"]
  }
}
```

### 에러 응답
```json
{ "message": "광고 내용(최소 10자) 또는 이미지/영상 파일 중 하나 이상 필요합니다." }
```

| 코드 | 의미 |
|------|------|
| 400 | 입력 유효성 오류 |
| 502 | 시뮬레이션 실행 오류 (OpenAI 실패) |
| 503 | 데이터베이스 오류 |
| 500 | 예상치 못한 오류 |

---

## 7. 설정 파일

### 백엔드 `requirements.txt`
```
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
openai>=1.0.0
pydantic>=2.7.0
asyncpg>=0.29.0
opencv-python-headless>=4.9.0
python-multipart>=0.0.9
python-dotenv>=1.0.0
```

### 프론트엔드 `package.json`
```json
{
  "dependencies": {
    "next": "^14.2.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
```

### `tailwind.config.js`
- 커스텀 색상 팔레트: brand (bg, bg2, text, muted, light)
- 폰트: Noto Sans KR + Nunito
- 커스텀 그림자: card, card-lg
- 블롭 애니메이션: 14초 ease-in-out

---

## 8. 실행 방법

### 백엔드
```bash
cd ai-engine
pip install -r requirements.txt
# .env 파일에 OPENAI_API_KEY, DB_* 설정
uvicorn main:app --host 0.0.0.0 --port 8000
```

### 프론트엔드
```bash
cd frontend
npm install
# .env.local에 NEXT_PUBLIC_API_URL=http://localhost:8000/api 설정
npm run dev
```

### 데이터베이스
- PostgreSQL 13+ 필요
- pgvector 확장 필요 (`CREATE EXTENSION vector`)
- 앱 시작 시 `init_memory_table()` 자동 실행

---

## 9. 컴포넌트-기능 대응표

| 컴포넌트/모듈 | 타입 | 핵심 역할 |
|---|---|---|
| `main.py` | FastAPI | HTTP API 서버, 요청 라우팅 |
| `agent.py` | 파이프라인 | 3단계 캐스케이드 시뮬레이션 엔진 |
| `db.py` | DB 레이어 | 페르소나·메모리 CRUD |
| `memory.py` | 시맨틱 검색 | 임베딩 기반 과거 기억 조회·저장 |
| `prompts.py` | 프롬프트 | 페르소나 정체성·광고 컨텍스트 프롬프트 |
| `vision.py` | 비전 분석 | 이미지/영상 → 광고 내용 보강 |
| `schemas.py` | 데이터 모델 | 전체 입출력 타입 정의 |
| `page.jsx` | 뷰 라우터 | 뷰 전환, 페르소나 전역 상태 |
| `SimulationDashboard` | React | 폼 입력, 결과 표시, 히스토리 |
| `WelcomeScreen` | React | 랜딩, 빠른 진입 |
| `PersonaCreator` | React | 페르소나 생성·편집 폼 |
| `MetricsPanel` | React | KPI 카드 표시 |
| `CognitiveTimeline` | React | 3단계 퍼널 시각화 |
| `PersonaFeedbackCard` | React | 개별 페르소나 반응 카드 |
| `simulationApi.js` | API 클라이언트 | fetch 래퍼 (JSON/multipart) |
| `useSimulation.js` | React 훅 | 시뮬레이션 상태 관리 |
